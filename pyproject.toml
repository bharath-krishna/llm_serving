[project]
name = "llm-serving"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "lmcache>=0.5.4; sys_platform == 'linux'",
    "openai>=3.3.1",
]
